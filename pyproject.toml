[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "drak-web"
version = "3.0.0"
description = "AI-Powered Dark Web OSINT & Threat Intelligence Platform"
readme = "README.md"
requires-python = ">=3.10"

[tool.vercel]
entrypoint = "app.py"
